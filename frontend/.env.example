# Leave blank in local Vite development to use the /api proxy.
# For a separately hosted frontend, set this to the public API origin.
VITE_API_BASE_URL=
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
